# Example environment variables for factcheck-ai-frontend

# Backend API URL (server-side only)
BACKEND_API_URL=https://factcheck-ai-backend.onrender.com

# API key for factcheck backend (server-side only, not exposed to browser)
FACTCHECK_API_KEY=your-api-key-here 